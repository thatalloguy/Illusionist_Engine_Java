package input;

import org.lwjgl.glfw.GLFW;
import renderEngine.DisplayManager;

public class KeyBoard {

	public Boolean getKey(String key) {
		
		if (key == "q") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_Q) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		if (key == "w") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_W) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "e") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_E) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "r") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_R) == GLFW.GLFW_PRESS) {
				return true;
			}
		}
		
		if (key == "t") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_T) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "y") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_Y) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		if (key == "u") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_U) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		if (key == "i") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_I) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		if (key == "o") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_O) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		if (key == "p") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_P) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		if (key == "a") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_A) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		if (key == "s") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_S) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		if (key == "d") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_D) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		if (key == "f") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_F) == GLFW.GLFW_PRESS) {
				return true;
			}
		}
		if (key == "g") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_G) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		if (key == "h") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_H) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "j") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_J) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "k") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_K) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "l") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_L) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "z") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_Z) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "x") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_X) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "c") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_C) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "v") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_V) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "b") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_B) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "n") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_N) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "m") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_M) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		
		if (key == "1") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_1) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "2") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_2) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "3") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_3) == GLFW.GLFW_PRESS) {
				return true;
			}
		}
		
		if (key == "4") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_4) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "5") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_5) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "6") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_6) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "7") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_7) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "8") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_8) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "9") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_9) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "0") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_0) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		
		
		
		if (key == "space") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_SPACE) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "lshift") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_LEFT_SHIFT) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "rshift") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_RIGHT_SHIFT) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		if (key == "lcontrol") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_LEFT_CONTROL) == GLFW.GLFW_PRESS) {
				return true;
			}
		}
		
		if (key == "rcontrol") {
			if (GLFW.glfwGetKey(DisplayManager.getRawWindow(), GLFW.GLFW_KEY_RIGHT_CONTROL) == GLFW.GLFW_PRESS) {
				return true;
			}
		} 
		
		return false;
	}
}
